function buttonClick(buttonElement){
    alert("hey you cliced"+buttonElement.textconContent
    );
    console.log("hey you clicked"+buttonElement.textconContent);

}

window.addEventListener("DOMContentLoaded",function(){
    var buttonslist = document.getElementsByClassName("button");
});
    for(const button of buttonslist){button.addEventListener("click", function(event); )}

const getData = (pageUrl){
    return new Promise((resolve, reject)
        let request = new XMLHttpRequest();
        request.onload = functionn(){
            if (this.readyState)
        }
    })
}